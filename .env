# DevSpace environment values
KUBE_CONTEXT=microk8s
KUBE_NAMESPACE=devspace
PRODUCTION_PROFILE=production

DEVSPACE_FLAGS=--kube-context ${KUBE_CONTEXT} --namespace ${KUBE_NAMESPACE}
DEVSPACE_DEPLOY_FLAGS=--profile ${PRODUCTION_PROFILE}